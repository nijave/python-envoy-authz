# VCS
.git/
.gitignore

# Python local envs / tooling
.venv/
venv/
.poetry-venv/

# Caches
__pycache__/
*.pyc
*.pyo
.ruff_cache/
.mypy_cache/
.pytest_cache/

# Tests and docs (not needed at runtime)
tests/
docs/

# IDE
.idea/
.vscode/

# Key material — never bake a locally generated OP signing key into the image
# (the Dockerfile does `COPY . /app`).
*.pem

# Repo infra
.woodpecker.yaml
.dockerignore
Dockerfile
k8s.yaml
renovate.json
